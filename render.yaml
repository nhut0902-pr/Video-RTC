services:
  - type: web
    name: rtc-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
      - key: POSTGRES_URL
        sync: false
      - key: JWT_SECRET
        sync: false
